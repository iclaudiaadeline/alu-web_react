# Webpack

Project covering Webpack fundamentals — bundling JavaScript with jQuery.

## task_0

Basic Webpack setup: bundles `src/index.js` (which imports jQuery) using Webpack's zero-config defaults. No custom `webpack.config.js` is used. The build output (`dist/main.js`) dynamically inserts three paragraphs into the page.