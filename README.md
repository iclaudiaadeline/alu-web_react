# alu-web_react

Project covering Webpack fundamentals as part of the ALU backend/advanced curriculum.

## Webpack

Basic Webpack setup and bundling exercises.

### task_0

Bundles a small JavaScript app with jQuery using Webpack's zero-config defaults (no custom `webpack.config.js`). The bundle output (`dist/main.js`) dynamically inserts three paragraphs into the page.